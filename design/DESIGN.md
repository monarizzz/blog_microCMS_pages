---
name: pre-design-md output
description: Generated by pre-design-md
colors:
  primary: "#80878d"
  primary-50: "#f9fafb"
  primary-100: "#f0f2f4"
  primary-200: "#dadfe2"
  primary-300: "#bfc5ca"
  primary-400: "#9ea6ab"
  primary-500: "#80878d"
  primary-600: "#636a6f"
  primary-700: "#494e52"
  primary-800: "#2f3336"
  primary-900: "#181b1d"
  primary-950: "#0a0b0d"
  neutral-50: "#f9fafb"
  neutral-100: "#f0f2f4"
  neutral-200: "#dadfe2"
  neutral-300: "#bfc5ca"
  neutral-400: "#9ea6ab"
  neutral-500: "#80878d"
  neutral-600: "#636a6f"
  neutral-700: "#494e52"
  neutral-800: "#2f3336"
  neutral-900: "#181b1d"
  neutral-950: "#0a0b0d"
  ac-success: "#6b8a6b"
  ac-warning: "#b8a17c"
  ac-danger: "#a0736f"
  ac-info: "#61859f"
typography:
  h1:
    fontFamily: "'Noto Sans JP', 'Hiragino Kaku Gothic ProN', system-ui, sans-serif"
    fontSize: 2.1773rem
    fontWeight: 700
    lineHeight: 1.3
  h2:
    fontFamily: "'Noto Sans JP', 'Hiragino Kaku Gothic ProN', system-ui, sans-serif"
    fontSize: 1.8144rem
    fontWeight: 700
    lineHeight: 1.3
  h3:
    fontFamily: "'Noto Sans JP', 'Hiragino Kaku Gothic ProN', system-ui, sans-serif"
    fontSize: 1.512rem
    fontWeight: 700
    lineHeight: 1.3
  h4:
    fontFamily: "'Noto Sans JP', 'Hiragino Kaku Gothic ProN', system-ui, sans-serif"
    fontSize: 1.26rem
    fontWeight: 700
    lineHeight: 1.3
  body:
    fontFamily: "'Noto Sans JP', 'Hiragino Kaku Gothic ProN', system-ui, sans-serif"
    fontSize: 0.875rem
    fontWeight: 400
    lineHeight: 1.6
  caption:
    fontFamily: "'Noto Sans JP', 'Hiragino Kaku Gothic ProN', system-ui, sans-serif"
    fontSize: 0.7292rem
    fontWeight: 400
    lineHeight: 1.6
spacing:
  2xs: 4px
  xs: 8px
  sm: 16px
  md: 24px
  lg: 32px
  xl: 48px
  2xl: 64px
  3xl: 96px
  4xl: 128px
  5xl: 192px
rounded:
  none: 0
  sm: 12px
  md: 24px
  lg: 36px
  xl: 48px
  full: 9999px
components:
  button:
    rounded: "{rounded.md}"
  card:
    rounded: "{rounded.lg}"
  input:
    rounded: "{rounded.sm}"
  badge:
    rounded: "{rounded.full}"
---

## Overview

Brand intent: monochrome (cool tint), compact.

Target context: general — adapt the values verbatim and elaborate on rationale where helpful.

Decisions made: typography, spacing, radius, shadow, color.

Dark mode is supported and dark variants of the palette are part of this spec.

## Colors

Color approach: monochrome (cool tint).

Monochrome with a barely-visible cool tint (warmth -1.00). Hierarchy still carried by lightness. The hex values in the frontmatter are derived from OKLCH source values; if you need the OKLCH form, see the Rich Prompt output.

### Interaction states (derived rules)

- `hover`: lightness −5%
- `active`: lightness −10%
- `focus`: 2px solid `{colors.primary}` with 2px offset
- `disabled`: opacity 40%

## Typography

Base font size: 14px. Modular scale ratio: 1.2 (minor third).

Single-family system (Noto Sans JP) — heading and body share the typeface for unity.

Google Noto Sans JP — universal Japanese sans, clean and clear.

## Spacing

Base unit: 8px. Scale approach: multiplicative.

Generous and bold — marketing-friendly breathing room.

## Rounded

Base radius: 24px (bold, almost-pill). Scale approach: scaled.

Per-component variation reinforces shape roles — inputs crisper, cards softer.

Per-component intent (resolved in the components block):
- input: `{rounded.sm}`
- button: `{rounded.md}`
- card: `{rounded.lg}`
- badge: `{rounded.full}`

## Components

Component-level shape choices are resolved via token references in the `components` block of the frontmatter. Future component additions should follow the same pattern: declare the role (button, card, input, …) and reference a `rounded` token rather than hard-coding a px value.

## Shadow

Shadow is not in the official YAML schema at alpha. Tokens are emitted as CSS variables for direct consumption:

```css
--shadow-sm: 0 1px 2px 0 oklch(25% 0.08 220 / 0.06);
--shadow-md: 0 4px 6px -1px oklch(25% 0.08 220 / 0.08), 0 2px 4px -2px oklch(25% 0.08 220 / 0.08);
--shadow-lg: 0 10px 15px -3px oklch(25% 0.08 220 / 0.1), 0 4px 6px -4px oklch(25% 0.08 220 / 0.1);
--shadow-xl: 0 20px 25px -5px oklch(25% 0.08 220 / 0.12), 0 8px 10px -6px oklch(25% 0.08 220 / 0.12);
```

Intensity: subtle. Tinted by the primary hue (`oklch(25% 0.08 220)`) for cohesion.

Elevation hierarchy: `dropdown` (sm) < `card` (md) < `modal` (lg) < `toast` (xl).

## Usage guidelines

- Primary actions use the `primary` color; on hover shift to `primary-600`, on active to `primary-700`.
- Cards take the `card` rounded role and a medium shadow.
- Buttons take the `button` rounded role and a small shadow on the primary variant.
- Inputs take the `input` rounded role; focus ring follows the interaction-states spec.
- Body text uses the `body` typography role.
- Section spacing: use the `3xl` spacing token between major blocks.
- Stack spacing within a block: use the `md` spacing token.

## Generation instructions for AI

1. Treat the listed values as authoritative; do not re-derive from preference.
2. When a component needs a value not listed here, pick the nearest available token.
3. If a new decision is required, document it in an appended section and flag it clearly.
4. Preserve the rationale comments — they encode design intent.
5. Interaction states are derived rules, not hard-coded values — apply them procedurally.