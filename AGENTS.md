# AGENTS.md

このリポジトリで作業するコーディングエージェント共通の指示。
指示はこのファイルに集約する。`CLAUDE.md` は本ファイルを読み込むだけで、内容を持たない。

## 基本ルール

- 必ず日本語で応答すること
- thinkingも日本語で行うこと
- Autoで実装する場合は、Commitまで実行しPushはユーザーが明示的に示した場合のみとする

## コミット

### 粒度

**1 コミット = 1 コンポーネント / 1 論点。**まとめて 1 コミットにしない。

- 新規コンポーネントは「実装 + `.stories.tsx` + 対応表の該当行」の 3 点で 1 コミット。
  ストーリーを別コミットにすると片方だけ revert できてしまう
- **既存コンポーネントへの変更は独立したコミットにする。**新規実装と同じコミットに埋めない。
  既存への波及を疑われる箇所ほど、単独で見えている必要がある
- ルーティング（`src/app/` 配下）の結線はコンポーネント実装と分ける。レビューの観点が違う
- 順序は依存の下から上へ。**各コミットの時点で `npx tsc --noEmit` が通る**こと

### メッセージ

**そのコミット単体で、何をどう変えたかが分かること。**

件名:

- 何を・どこに・どうしたかを書く。`実装` `追加` だけで終わらせない
- 既存改修なら「〜に〜を追加」と、新規と書き分ける
- 全コミットに共通する枕詞（`ui.pen の` など）は件名に置かない。区別に寄与せずノイズになる

本文（新規コンポーネント・判断を伴う変更には必須）:

- 実装したバリアント・状態の数
- **推測で埋めた箇所、自分で決めたロジック。**ここが最も重要。
  書き残さないと、後から「なぜこの形か」を追えなくなる
- 既存コンポーネントへ影響が無いなら、無いと明記する

```
feat FilterBtn に size prop を追加

ソート行は Tag (biM87) の padding override (6/12)。
CHIP_CLASS_NAME を書き換えると TagBtn を巻き添えにするため、
既定 (sm) を変えずに md を足す形にした。TagBtn の見た目は変わらない。
```

## worktree

作業用に worktree を作った場合、**その作業が終わった時点で必ず削除する**こと。

```bash
git worktree remove <パス>   # 空になった親ディレクトリも消す
git worktree list            # 消えたことを確認
```

- 消すのは worktree だけで、ブランチは残す
- 削除前に `git status` で未コミットの変更が無いことを確認する
- 放置するとリポジトリ全体の複製がツリー内に残り、検索やレビューのノイズになる

## コードレビューの言語

- レビューコメントは**必ず日本語で記述すること**
- 指摘のタイトル・本文・要約(サマリ)のすべてを日本語にすること
- コード片・識別子・ファイルパス・エラーメッセージの引用は原文のまま残してよい

## レビューの観点

- バグや不具合につながる実装
- パフォーマンス上の問題
- セキュリティ上の懸念
- テストの不足

## プロジェクトの前提

- Next.js (App Router) + TypeScript + Tailwind CSS の構成
- スタイルは `src/styles/tokens.css` / `src/styles/globals.css` のデザイントークンを使用する
